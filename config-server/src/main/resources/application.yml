server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/studiojms/spring-cloud-microservice-data
          search-paths: provider, store, transporter
